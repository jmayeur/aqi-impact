{
  "name": "aqi-impact",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "workspaces": [
    "site"
  ],
  "scripts": {
    "dev": "npm run dev --workspace site",
    "build": "npm run build --workspace site",
    "fetch": "tsx --env-file=.env scripts/fetch-and-score.ts",
    "inspect": "tsx --env-file=.env scripts/fetch-and-score.ts --inspect",
    "score": "tsx --env-file=.env scripts/score-from-sqlite.ts",
    "backfill": "tsx --env-file=.env scripts/backfill.ts",
    "backfill-from-api": "tsx --env-file=.env scripts/backfill-from-api.ts"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "better-sqlite3": "^12.10.0"
  }
}
